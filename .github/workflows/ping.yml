name: Ping buggy app

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Hit endpoints
        run: |
          curl -s https://buggy-app.onrender.com/
          curl -s https://buggy-app.onrender.com/sometimes-fails
          curl -s https://buggy-app.onrender.com/auth